---
- hosts: localhost
  connection: local
  tasks:
    - docker_container:
        name: myhello
        image: kite01/myhello:latest
        state: started
        ports:
          - "80:8080"
...

